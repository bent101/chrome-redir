{
	"name": "focus-redirect",
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"build": "bun run index.ts && bun build ./src/background.ts --outdir .",
		"watch": "bun run index.ts --watch"
	},
	"devDependencies": {
		"@types/chrome": "^0.0.260",
		"@types/bun": "latest",
		"typescript": "^5.0.0",
		"@types/node": "^22.9.0"
	},
	"module": "index.ts"
}
